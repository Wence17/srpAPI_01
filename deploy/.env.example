# =============================================================================
# DEPRECATED — use the repo root `.env.example` instead
# =============================================================================
#
#   cp ../.env.example ../.env
#
# Docker Compose loads variables from the repo root `.env`:
#
#   docker compose --env-file ../.env -f docker-compose.local.yml up -d
#
# This file is kept as a pointer for existing documentation links.
# =============================================================================
